<h1 class="">New Food</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.food_path(@conn, :create)) %>

<span><%= link "Cancel", to: Routes.food_path(@conn, :index), class: "btn btn-outline-primary btn-fixed-width" %></span>
